{
  "name": "multimodal-article-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile-first viewer for article, Google Slides, and NotebookLM destinations.",
  "scripts": {
    "dev": "node dev-server.js",
    "check": "node --check app.js && node --check dev-server.js",
    "test": "node --test tests/rules.contract.test.mjs && playwright test",
    "test:e2e": "playwright test",
    "playwright:install": "playwright install chromium",
    "playwright:verify": "node scripts/verify-playwright.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
